[build]
command = "echo Building Functions"

[functions]
  external_node_modules = ["express"]
  node_bundler = "esbuild"
[[redirects]]
  force = true
  from = "/api/games/*"
  status = 200
  to = "/.netlify/functions/api/games/:splat"
